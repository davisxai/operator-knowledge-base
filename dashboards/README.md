# Dashboards

Dashboard builds from the content. Mission controls for AI agents, business ops views, client panels. Each build goes from a template to a live URL with a real database behind it.

Nothing here yet. The first build, a mission control for AI agents built on a free shadcn template, is in production for an upcoming post. It lands here when the video ships.
